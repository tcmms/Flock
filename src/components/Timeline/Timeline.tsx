import { Timeline as AntTimeline } from 'antd'
import type { TimelineProps } from 'antd'

/**
 * Timeline — vertical timeline display.
 * Built on Ant Design Timeline with Flock DS tokens via FlockProvider.
 * Supports all Ant Design Timeline props.
 */
export { AntTimeline as Timeline }
export type { TimelineProps }
