/**
 * Breadcrumb component — a wrapper around Ant Design's Breadcrumb.
 *
 * Displays the current page location within a navigational hierarchy.
 * Supports custom separators, icons, and dropdown menus.
 *
 * @see https://ant.design/components/breadcrumb
 */
export { Breadcrumb } from 'antd';
export type { BreadcrumbProps } from 'antd';
