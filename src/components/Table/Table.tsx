import { Table as AntTable } from 'antd'
import type { TableProps } from 'antd'

/**
 * Table — displays rows of data with sorting, filtering, pagination.
 * Built on Ant Design Table with Flock DS tokens via FlockProvider.
 * Use FlockIcons for custom expandIcon, filterIcon in column definitions.
 */
export { AntTable as Table }
export type { TableProps }
