import { Carousel as AntCarousel } from 'antd'
import type { CarouselProps } from 'antd'

/**
 * Carousel — rotating content display.
 * Built on Ant Design Carousel with Flock DS tokens via FlockProvider.
 * Supports all Ant Design Carousel props.
 */
export { AntCarousel as Carousel }
export type { CarouselProps }
