import { Popover as AntPopover } from 'antd'
import type { PopoverProps } from 'antd'

/**
 * Popover — floating card triggered by click, hover, or focus.
 * Built on Ant Design Popover with Flock DS tokens via FlockProvider.
 * Supports all Ant Design Popover props.
 */
export { AntPopover as Popover }
export type { PopoverProps }
