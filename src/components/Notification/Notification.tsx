import { notification } from 'antd'

/**
 * Notification — global notification message.
 * Built on Ant Design notification with Flock DS tokens via FlockProvider.
 * Use Notification.success(), Notification.error(), etc. for static API.
 */
export { notification as Notification }
