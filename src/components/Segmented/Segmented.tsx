import { Segmented as AntSegmented } from 'antd'
import type { SegmentedProps } from 'antd'

/**
 * Segmented — segmented control for switching between options.
 * Built on Ant Design Segmented with Flock DS tokens via FlockProvider.
 * Supports all Ant Design Segmented props.
 */
export { AntSegmented as Segmented }
export type { SegmentedProps }
