import { message } from 'antd'

/**
 * Message — global message notification.
 * Built on Ant Design message with Flock DS tokens via FlockProvider.
 * Use Message.success(), Message.error(), etc. for static API.
 */
export { message as Message }
