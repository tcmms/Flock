import { Result as AntResult } from 'antd'
import type { ResultProps } from 'antd'

/**
 * Result — display operation result feedback.
 * Built on Ant Design Result with Flock DS tokens via FlockProvider.
 * Supports all Ant Design Result props.
 */
export { AntResult as Result }
export type { ResultProps }
