import { Slider as AntSlider } from 'antd'
import type { SliderSingleProps } from 'antd'

/**
 * Slider — slider input control for selecting a value or range.
 * Built on Ant Design Slider with Flock DS tokens via FlockProvider.
 * Supports all Ant Design Slider props.
 */
export { AntSlider as Slider }
export type { SliderSingleProps as SliderProps }
